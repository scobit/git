# установка на debian
sudo apt-get install git

# установка RedHat
sudo yum install git

# проверить версию
git --version

# установка имени и почты
git config --global user.name "Alexey Ilin"
git config --global user.email "alexey.ilin@gmail.com"

# отображение текущего имени и почты
git config --global user.name
git config --global user.email

# проверка текущей конфигурации
git config -l 

# ручное редактирование конфигурации в файле
vim .gitconfig

# файл конфигурации локального репозитория
vim .git

# общая справка
git --help

# мини обучение
git help tutorial

# 3 уровня конфигурации git
1 покрывает всех пользователей системы - git config --system
2 относится к настройкам конкреткного пользователя системы - git config --global
3 относится к текущему репозиторию - git config or git config --local


